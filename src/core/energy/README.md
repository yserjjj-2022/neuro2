# Free Energy & Affective Signals

Расчёт свободной энергии F(t), валентности (-dF/dt),
аллостатического стресса и точности (precision).
Event-triggered inference — расчёт только при F(t) > threshold.
