# Lateral Voting (k-WTA)

Латеральное торможение для консенсуса между колонками.
k-WTA алгоритм без центрального контроллера.

Сложность: Фаза 1 — O(N log N) (stable argsort). O(N) — целевое свойство
Фазы 3 (np.argpartition), не текущая гарантия.
